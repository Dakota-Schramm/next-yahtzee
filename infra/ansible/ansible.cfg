[defaults]
inventory         = inventory.ini
remote_user       = ubuntu
host_key_checking = False
result_format     = yaml
roles_path        = roles

[privilege_escalation]
become      = False
become_method = sudo

[ssh_connection]
pipelining = True
