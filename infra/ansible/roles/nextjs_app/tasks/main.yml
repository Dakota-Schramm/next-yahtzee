#SPDX-License-Identifier: MIT-0
---
# tasks file for nextjs_app
- name: Create app directory
  become: true
  file:
    path: /srv/app
    state: directory
    owner: "{{ ansible_user }}"
    mode: '0755'
- name: Sync app source to target
  synchronize:
    src: "{{ playbook_dir }}/../../../"   # project root
    dest: /srv/app/
    rsync_opts:
      - "--exclude=node_modules"
      - "--exclude=.next"
      - "--exclude=.git"
- name: install deps
  shell: cd /srv/app/ && yarn install 
- name: build app
  shell: cd /srv/app/ && yarn build
- name: run app
  shell: pm2 startOrRestart .next/standalone/server.js --name yahtzee
  args:
    chdir: /srv/app
